# Leave both empty to use librespot's public client. Its Web API quota is
# shared by every librespot user, so search and likes answer 429 — an own app
# lifts that. Register the redirect below in its dashboard.
SPT_CLIENT_ID=
SPT_REDIRECT_URI=
